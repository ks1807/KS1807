<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>

    <style name="popUpOptions" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@color/white</item>
        <item name="android:itemBackground">@drawable/dropdownback</item>
        <item name="android:fontFamily">@font/roboto_medium</item>
        <item name="android:layout_width">100dp</item>

    </style>
    <style name="overlaytheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/overlay</item>
        <item name="android:clickable">false</item>
        <item name="android:padding">5dp</item>
    </style>
</resources>
